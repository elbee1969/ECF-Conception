let url = "http://localhost:9090";
let endPointForum = "/api/forum";

const submitF = document.getElementById('submitForm');
const myForm = document.getElementById('forum');
let form = ('forum');

eventListening(submitF,myForm,url,endPointForum,form);